import styled from '@emotion/styled';
import { InputLabel } from '@mui/material';

import './StoreInputLabel.scss';

export const StoreInputLabel = styled(InputLabel)({
	color: '#243573',
	fontWeight: 700,
	width: 300,
});
